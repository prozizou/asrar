# Production — base des fonctions serverless réutilisées (proxy /api/* → ici).
# Le backend reste celui du site existant (site statique + fonctions Vercel).
# Valeur publique (URL uniquement) : rien de sensible ici.
NEXT_PUBLIC_API_BASE=https://asrar-hub.vercel.app
